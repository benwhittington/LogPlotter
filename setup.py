from setuptools import find_packages, setup

setup(
    name='LogPlotter',
    packages=find_packages(include=["LogPlotter"]),
    version='0.1.0',
    description='Returns list of values from text file based on regex',
    author='ben.whittington',
)